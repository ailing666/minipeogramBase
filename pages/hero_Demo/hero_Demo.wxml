<view class="box">
	<navigator class="item" wx:for="{{heroList}}" wx:key="id" url="/pages/daily_Demo/daily_Demo?skill={{item.skill}}">
		<image src="{{item.icon}}"></image>
		<text bindtap="click"> {{item.heroName}} </text>
	</navigator>
</view>